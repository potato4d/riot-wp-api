{
  "name": "riot-wp-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "browserify -t riotify ./src/main.js -o res/bundle.js && browser-sync start --server --files 'res/*.js'",
    "build": "browserify -t riotify ./src/main.js -o res/bundle.js"
  },
  "author": "Potato4d",
  "license": "MIT",
  "dependencies": {
    "jquery": "^3.1.0",
    "riot": "^2.5.0",
    "whatwg-fetch": "^1.0.0"
  },
  "devDependencies": {
    "browser-sync": "^2.13.0",
    "browserify": "^13.0.1",
    "riotify": "^1.0.1"
  }
}
